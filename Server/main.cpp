#include "Server.h"

using namespace std;

int main() {
    //std::cout << INVALID_SOCKET << std::endl;
    Server serv;
    string input = "";
    do {
        cin >> input;
    } while (input != "quit");
}